[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm prisma generate && pnpm run build:ts""

[deploy]
startCommand = "fastify start -l info -a 0.0.0.0 dist/src/app.js"
healthcheckPath = "/"
healthcheckTimeout = 100

