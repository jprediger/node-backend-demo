#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd backend || exit 1

# build + testes com cobertura (ajuste se quiser algo mais leve)
echo "Pre-push hook executando..."
pnpm test
echo "Pre-push hook concluído"